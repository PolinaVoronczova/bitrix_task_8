<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.min.css">
    <title>Переименовать файл</title>
</head>
<body>
    <h1>Переименовать файл</h1>
    
<form action="/files/rename" method="post">
    <input type="hidden" name="current_path" value="<?= htmlspecialchars($currentPath) ?>">
    
    <div class="form-group">
        <label>Текущее имя:</label>
        <input type="text" class="form-control" value="<?= htmlspecialchars($currentName) ?>" readonly>
    </div>
    
    <div class="form-group">
        <label>Новое имя (без пути):</label>
        <input type="text" class="form-control" name="new_name" 
               value="<?= htmlspecialchars($currentName) ?>" required
               pattern="[^\/\\]+" title="Нельзя использовать символы / и \">
    </div>
    
    <button type="submit" class="btn btn-primary">Переименовать</button>
    <a href="/files" class="btn btn-secondary">Отмена</a>
</form>
</body>
</html>