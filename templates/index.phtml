<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.min.css">
    <title>Главная страница</title>
</head>
<body>
    <h1>Проверка Яндекс.Диска</h1>

        <style>
        .alert {
            padding: 15px;
            margin: 10px 0;
            border: 1px solid transparent;
            border-radius: 4px;
        }
        .alert-success {
            color: #3c763d;
            background-color: #dff0d8;
            border-color: #d6e9c6;
        }
        .alert-error {
            color: #a94442;
            background-color: #f2dede;
            border-color: #ebccd1;
        }
    </style>

    <?php if (!empty($flash)): ?>
        <?php foreach ($flash as $type => $messages): ?>
            <?php foreach ($messages as $message): ?>
                <div class="alert alert-<?= $type ?>"><?= htmlspecialchars($message) ?></div>
            <?php endforeach ?>
        <?php endforeach ?>
    <?php endif ?>
    
    <p>Статус: <?= htmlspecialchars($connection) ?></p>
    <p><a href="/files" class="btn btn-secondary">Перейти к списку файлов</a></p>
</body>
</html>